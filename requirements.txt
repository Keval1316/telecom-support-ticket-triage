# =========================================================
# LOCAL environment — data generation, backend, testing
# (No GPU/CUDA packages here — training runs on Colab, see requirements-colab.txt)
# =========================================================
python-dotenv==1.0.1
pydantic==2.8.2
pandas==2.2.2
numpy==1.26.4
tqdm==4.66.4

# Data generation (Groq)
groq==0.9.0

# Light ML utils used outside training (metrics, splitting)
scikit-learn==1.5.1

# Backend (FastAPI)
fastapi==0.112.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.32
python-multipart==0.0.9
httpx==0.27.0

# Dev / Testing
pytest==8.3.2
pytest-asyncio==0.24.0
